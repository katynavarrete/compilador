Program Conductor_de_Prueba;
Var
  c,h,r: integer;
  r2:boolean;

procedure pru2(p: boolean);
begin
  {pru:=18}
  r := 4;
end;

Begin
  read(c);
{((c > -(5*-4*r))}
  pru2((c > -(5*-4*r)){or (3<>5) and true or (r>2) } );
  if r2 then
  begin
     write(1)
  end
  else
     write(0);

End.
